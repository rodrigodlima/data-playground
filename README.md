# data-playground
Repository for experimenting with data solutions, including data pipelines, data processing and more

# How to running

This POC is under construction. But, to run all services, including nginx as reversy proxy and observability, run:

```
$ docker compose --profile observability --profile proxy up -d
```
